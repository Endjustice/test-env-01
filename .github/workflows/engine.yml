name: Core Health Check
on:
  workflow_dispatch:
jobs:
  run-analysis:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run AI Node
        run: |
          chmod +x setup.sh
          ./setup.sh
